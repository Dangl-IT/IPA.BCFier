import { GroupingTypePipe } from './grouping-type.pipe';

describe('GroupingTypePipe', () => {
  it('create an instance', () => {
    const pipe = new GroupingTypePipe();
    expect(pipe).toBeTruthy();
  });
});
